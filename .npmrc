# Publish @axiora-ui/* scoped packages as public by default
@axiora-ui:registry=https://registry.npmjs.org/
access=public

# pnpm workspace publishing — replace workspace: protocol with real versions
publish-workspace-packages=true
